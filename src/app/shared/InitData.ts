export class InitData {

  public constructor(public width: number, public height: number, public aliveRatio: number, public born: number[],
                     public stable: number[], public dead: number[]) {
  }
}
